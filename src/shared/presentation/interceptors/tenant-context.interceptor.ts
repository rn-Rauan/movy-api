/**
 * @deprecated This file is dead code.
 *
 * TenantContext is created by JwtAuthGuard, not by middleware or interceptor.
 * Kept only for backward-compatible type re-export.
 */

export type { TenantContext } from 'src/shared/infrastructure/types/tenant-context.interface';
