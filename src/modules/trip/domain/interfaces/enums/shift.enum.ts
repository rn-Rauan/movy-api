/**
 * Time-of-day classification for a {@link TripTemplate}.
 *
 * Used as a searchable label that allows passengers to filter trips
 * by the part of the day they prefer to travel.
 */
export enum Shift {
  MORNING = 'MORNING',
  AFTERNOON = 'AFTERNOON',
  EVENING = 'EVENING',
}
